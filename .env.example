# ตัวอย่างการตั้งค่าสำหรับ FuelTracker
# เส้นทางไปยังฐานข้อมูล SQLite ที่โปรแกรมใช้
# ค่าเริ่มต้นคือ appdirs.user_data_dir("FuelTracker", "YourOrg")/fuel.db
DB_PATH=fuel.db
# เลือกธีมที่รองรับ ได้แก่ light, dark, modern และ vivid
FT_THEME=light

# รหัสผ่าน SQLCipher (เว้นว่างไว้หากไม่ต้องการเข้ารหัส)
FT_DB_PASSWORD=

# URL หลักสำหรับ API ราคาน้ำมัน
OIL_API_BASE=

# จำนวนชั่วโมงระหว่างการอัปเดตราคาน้ำมันและการตรวจอัปเดตอัตโนมัติ (ตั้งค่า 0 เพื่อปิด)
UPDATE_HOURS=24
# เส้นทางโฟลเดอร์คลาวด์สำหรับสำรองข้อมูล
FT_CLOUD_DIR=
