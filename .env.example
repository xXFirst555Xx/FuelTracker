# Example configuration for FuelTracker
# Path to the SQLite database used by the application
DB_PATH=fuel.db
# Theme selection: light, dark, or modern
FT_THEME=light

# SQLCipher password (leave empty for no encryption)
FT_DB_PASSWORD=
